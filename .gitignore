# Secrets — NIEMALS committen
.mcp.json
.codex/config.toml
opencode.json
.env
.env.local
.env*.local

# Dependencies / Build
node_modules/
.next/
out/
dist/
__pycache__/
*.pyc
.venv/
venv/

# Claude Code lokal
.claude/settings.local.json
backup/

# OS
.DS_Store
Thumbs.db

# Backend-Export und daraus erzeugte Zuordnungstabellen:
# enthalten dienstliche E-Mail-Adressen von Kolleg:innen — nicht ins Repo.
oz-logik/daten/
PAGES-PrintOnDemand_*.xlsx

# Maschinenspezifisch / generiert
.claude/launch.json
*.tsbuildinfo

# Event-Passwort für den buildbar-Deploy — nie ins Repo
.event-passwort

# Data-Table-IDs gelten nur fuer eine bestimmte n8n-Instanz.
# Erzeugt von oz-logik/baue-tabellen.js — siehe docs/uebergabe.md.
oz-logik/tabellen.json
oz-logik/credentials.json
