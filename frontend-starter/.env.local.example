# Basis-URL deiner n8n-Instanz (für Muster A — Webhook-Aufruf aus dem Frontend)
NEXT_PUBLIC_N8N_BASE=https://DEINE-INSTANZ.app.n8n.cloud

# Nur für den optionalen KI-Chat (Muster C). Ohne diesen Key funktioniert /chat nicht.
ANTHROPIC_API_KEY=
